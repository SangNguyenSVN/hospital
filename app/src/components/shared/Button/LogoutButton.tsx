import React from 'react';

const LogoutButton = () => {
   

    const handleLogout = () => {
       
    };

    return <button onClick={handleLogout}>Logout</button>;
};

export default LogoutButton;
